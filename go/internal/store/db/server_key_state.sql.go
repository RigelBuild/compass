// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: server_key_state.sql

package db

import (
	"context"
)

const getServerKeyState = `-- name: GetServerKeyState :one

SELECT id, key_version, key_fingerprint, fingerprint_salt, updated_at
FROM server_key_state WHERE id = 1
`

// server_key_state queries: the master-key tripwire. Single-row by construction
// (CHECK (id = 1)); GetServerKeyState reads it, InsertServerKeyState writes it
// once at first boot. No UPDATE and no DELETE here: rotation is a later
// record's versioned re-encrypt. The tripwire catches an operator booting the
// wrong key, not an actor with write access to this table -- UPDATE is granted,
// so a write-capable actor could restate the digest.
func (q *Queries) GetServerKeyState(ctx context.Context) (ServerKeyState, error) {
	row := q.db.QueryRow(ctx, getServerKeyState)
	var i ServerKeyState
	err := row.Scan(
		&i.ID,
		&i.KeyVersion,
		&i.KeyFingerprint,
		&i.FingerprintSalt,
		&i.UpdatedAt,
	)
	return i, err
}

const insertServerKeyState = `-- name: InsertServerKeyState :exec
INSERT INTO server_key_state (id, key_version, key_fingerprint, fingerprint_salt)
VALUES (1, $1, $2, $3)
`

type InsertServerKeyStateParams struct {
	KeyVersion      int16
	KeyFingerprint  []byte
	FingerprintSalt []byte
}

func (q *Queries) InsertServerKeyState(ctx context.Context, arg InsertServerKeyStateParams) error {
	_, err := q.db.Exec(ctx, insertServerKeyState, arg.KeyVersion, arg.KeyFingerprint, arg.FingerprintSalt)
	return err
}
