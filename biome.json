{
	"$schema": "https://biomejs.dev/schemas/2.5.4/schema.json",
	"root": true,
	"vcs": { "enabled": true, "clientKind": "git", "useIgnoreFile": true },
	"files": { "includes": ["**", "!**/gen", "!forks/*", "!**/*.grit"] },
	"formatter": { "indentStyle": "tab" },
	"plugins": ["tools/biome-plugins/no-fixed-sleep.grit"],
	"linter": {
		"enabled": true,
		"domains": {
			"test": "recommended",
			"project": "recommended"
		},
		"rules": {
			"style": {
				"noRestrictedImports": {
					"level": "error",
					"options": {
						"paths": {
							"@connectrpc/connect": "Reach the daemon only through @compass/client — the owned compass.v1 door. Raw stub access is fenced.",
							"@connectrpc/connect-web": "Reach the daemon only through @compass/client — the owned compass.v1 door. Raw transport access is fenced.",
							"@connectrpc/connect-node": "Reach the daemon only through @compass/client — the owned compass.v1 door. Raw transport access is fenced (the agent's Runner-socket transport is the one scoped exception)."
						},
						"patterns": [
							{
								"group": [
									"@connectrpc/connect/**",
									"@connectrpc/connect-web/**",
									"@connectrpc/connect-node/**"
								],
								"message": "Reach the daemon only through @compass/client — the owned compass.v1 door."
							}
						]
					}
				},
				"noRestrictedGlobals": {
					"level": "error",
					"options": {
						"deniedGlobals": {
							"setTimeout": "Fixed sleeps/timers are non-deterministic and flake tests (rule://go-no-sleep-in-test, same discipline in TS). Event-gate on the observed state; use Effect.sleep (TestClock-testable) in Effect code. A genuine timer (fetch-abort timeout, abortable backoff) justifies itself with `// biome-ignore lint/style/noRestrictedGlobals: <reason>`.",
							"setInterval": "Polling on a wall-clock interval is non-deterministic and flakes tests. Event-gate on the observed state; or `// biome-ignore lint/style/noRestrictedGlobals: <reason>` for a genuine production interval."
						}
					}
				}
			},
			"nursery": {
				"noFloatingPromises": "error",
				"noMisusedPromises": "error"
			},
			"correctness": {
				"noUndeclaredDependencies": "error"
			},
			"suspicious": {
				"noConsole": "error"
			},
			"complexity": {
				"noExcessiveCognitiveComplexity": "warn"
			}
		}
	},
	"assist": {
		"actions": {
			"source": {
				"organizeImports": "on"
			}
		}
	},
	"overrides": [
		{
			"includes": ["packages/compass-client/**"],
			"linter": {
				"rules": {
					"style": { "noRestrictedImports": "off" }
				}
			}
		},
		{
			"includes": ["packages/compass-agent/src/transport/**"],
			"linter": {
				"rules": {
					"style": { "noRestrictedImports": "off" },
					"nursery": { "noFloatingPromises": "off", "noMisusedPromises": "off" }
				}
			}
		},
		{
			"includes": [
				"**/tools/**",
				"**/scripts/**",
				"packages/compass-agent/src/cli.ts",
				"**/*.test.{ts,tsx}"
			],
			"linter": {
				"rules": {
					"suspicious": { "noConsole": "off" }
				}
			}
		}
	]
}
